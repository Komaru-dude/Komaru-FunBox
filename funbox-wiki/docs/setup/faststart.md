# 🚀 Быстрый старт

## 📌 Как начать пользоваться ботом

1. **Выдайте боту права администратора**  
   Это **не обязательно**, но **очень рекомендуется** — иначе вам придётся писать команды в виде `/ai@komarufunbox_bot`, а не просто `/ai`. Также без прав не будет работать модерация и часть функций.

   Минимальные права для корректной работы:
   - Удаление сообщений  
   - Блокировка пользователей  
   - Закрепление сообщений

2. **Назначьте себя администратором бота**  
   Создатель чата должен выполнить команду:

   ```
   /set_rank
   ```

---

## ⚙️ Команды бота

### 🛠 Администрирование и модерация

| Команда         | Описание |
|----------------|----------|
| `/set_rank`    | Назначение прав (ранга) пользователю |
| `/info`        | Получение информации о пользователе |
| `/history`     | История наказаний (варны, мьюты, баны) |
| `/warn`        | Выдать предупреждение |
| `/mute`        | Мьют пользователя на время |
| `/unmute`      | Снять мьют |
| `/ban`         | Забанить пользователя |
| `/unban`       | Разбанить пользователя |
| `/ban_media`   | Запрет медиа-функций для пользователя |
| `/unban_media` | Разрешить медиа-функции |
| `/enable`      | Включить модуль |
| `/disable`     | Выключить модуль |

---

### 🤖 Команды с ИИ

| Команда         | Описание |
|----------------|----------|
| `/ai`          | Запрос к ИИ. Можно указать модель через `-m`, например `/ai -m searchgpt` |
| `/agai`        | "Злая" версия — отвечает с агрессией и матом. Поддержка моделей через `-m` |
| `/chat`        | Начать диалог с ИИ. Поддержка споров через `-argue` и моделей через `-m` |
| `/chat_stop`   | Остановить текущий ИИ-чат |
| `/set_def_model` | Установить модель ИИ по умолчанию |
| `/available_models` | Список доступных ИИ моделей |
| `/ocr`         | Распознать текст на картинке |
| `/translate`   | Перевод текста с указанием языка (`/translate en Привет`) |

---

### 🖼️ Генерация и медиа

| Команда         | Описание |
|----------------|----------|
| `/image`       | Генерация картинки по описанию |
| `/video`       | Скачать видео (YouTube, TikTok и др.) |
| `/jpeg`        | "Шакализация" картинки |

---

### 🐱 Развлечения

| Команда         | Описание |
|----------------|----------|
| `/weather`     | Погода в городе, например: `/weather Москва` |
| `/http_cat`    | Рандомный HTTP-кот. Можно указать код: `/http_cat 404` |
| `/http_dog`    | То же самое, но с собаками |
| `/cat`         | Случайная фотка кота |
| `/cat_gif`     | Случайная гифка с котом |
| `/shutter`     | Преобразует текст в "залипашки", например: `/shutter Привет` |
| `/new_year`    | Сколько осталось до Нового года |
| `/birthdays`   | Когда день рождения у котиков |

---

### 📜 Ролевые команды

| Команда         | Описание |
|----------------|----------|
| `/rp_setup`    | Установить стандартные рп-команды |
| `/rp_add`      | Добавить свою рп-команду |
| `/rp_list`     | Посмотреть все рп-команды |
| `/rp_remove`   | Удалить одну рп-команду |
| `/rp_wipe`     | Удалить все рп-команды в чате |

---

## 🧩 Модули

Модули — это дополнительные функции, которые можно включать или выключать через команды `/enable` и `/disable`.

## 📋 Список доступных модулей

| Модуль        | Описание |
|---------------|----------|
| `who`         | Если кто-то ответит «это что?» — бот спросит у нейросети и ответит |
| `tag`         | Любой участник сможет тегать всех через `/tag` и `/tagall` |
| `autovideo`   | Автоматическая загрузка видео при отправке ссылки |
| `warn`        | Включает /warn |
| `mute`        | Включает /mute |
| `ban`         | Включает /ban |
| `senddisabledmsg` | Показывает сообщение при попытке использовать отключённую функцию |

👉 **Пример включения модуля:**  

```
/enable who
```

👉 **Пример отключения модуля:**  

```
/disable tag
```

---

## ℹ️ Примечания

- ⚠️ Некоторые команды (например `/update`, `/restart`, `/ban_media`) доступны **только персоналу**.
- 🧠 ИИ-команды используют [OnlySQ API](https://api.onlysq.ru/)
- 🔒 Команду `/set_rank` может использовать только **создатель чата**
- 📆 Актуально на **13.05.2025**

---

Автор: [@komaru_Dude](https://t.me/komaru_Dude)  
Помощь с документацией: [@nillerxs](https://t.me/nillerxs)
